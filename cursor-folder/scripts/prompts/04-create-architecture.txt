@.tasks/todo/task-NNN.md

## Выполни задачу из task-NNN.md

Создай новую архитектурную структуру (слои, компоненты, страницы или модули).

Принципы:
- Разделяй UI и бизнес-логику (по возможности выноси логику в кастомные хуки `use...`).
- Стили выноси в `*.module.css` (согласно правилам проекта) или используй возможности Mantine (props).
- При необходимости создай типы в отдельном файле (например `[Name]Types.ts`).
- Следуй правилам `code-style.md` в проекте.

НЕ создавай тесты, если в задаче явно не указано `TESTS_REQUIRED: true`.
НЕ создавай документацию, SUMMARY, CHECKLIST.

После: "✅ Структура создана/обновлена"

РЕЖИМ: Agent Mode