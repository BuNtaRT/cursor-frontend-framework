@.tasks/todo/task-NNN.md

## Выполни задачу из task-NNN.md

Запусти проверку линтера и типов в терминале:

```bash
yarn lint
yarn tsc --noEmit
```
(Если в проекте используется другой пакетный менеджер, например `npm` или `pnpm`, используй его команды).

Если найдены ошибки линтера или TypeScript, которые возникли в измененных нами файлах:
1. Проанализируй их.
2. Внеси исправления в код.
3. Запусти проверку еще раз, чтобы убедиться, что ошибки ушли.

Если ошибок нет, просто заверши задачу.

Создай или измени ТОЛЬКО исправленный код.
НЕ создавай отчеты: REPORT*.md, SUMMARY*.md, CHECKLIST*.md

После: "✅ Линтер и TS проверены, ошибки исправлены"

РЕЖИМ: Agent Mode