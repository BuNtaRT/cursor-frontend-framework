@.tasks/todo/task-NNN.md

## Выполни задачу из task-NNN.md

Прочитай задачу и выполни рефакторинг указанных файлов.

### Принципы:
- Не меняй поведение приложения для пользователя (UI должен работать так же, если не указано иное).
- Каждое изменение должно быть атомарным.

### Типичные задачи рефакторинга в React:
- Вынести повторяющийся код в кастомный хук (`useSomething`).
- Разбить большой "God-компонент" на мелкие.
- Вынести общие интерфейсы/типы в отдельный файл.
- Оптимизация производительности: `useMemo`, `useCallback`, или устранение лишних ререндеров (убираем `setState` из эффектов).
- Устранить "magic numbers" и хардкод-строки, вынести в константы.

Создай или измени ТОЛЬКО нужные файлы.
НЕ создавай тесты, если нет тега `TESTS_REQUIRED: true`.
НЕ создавай отчеты: REFACTOR*.md, SUMMARY*.md, CHECKLIST*.md

После: "✅ Рефакторинг выполнен: [одна строка]"

РЕЖИМ: Agent Mode