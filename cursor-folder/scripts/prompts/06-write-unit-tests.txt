@.tasks/todo/task-NNN.md

## Выполни задачу из task-NNN.md

Напиши unit-тесты для указанного компонента, хука или утилиты.

Создай ТОЛЬКО тестовые файлы, например `[Name].test.tsx` или `[Name].spec.ts` рядом с тестируемым файлом (или в папке `__tests__` согласно структуре проекта).

### Тесты должны покрывать:
- Ожидаемый рендер (Happy path)
- Пользовательские взаимодействия (fireEvent / userEvent)
- Краевые случаи (отсутствие пропсов, ошибки API, пустые массивы)

### Инструменты (React):
- Vitest или Jest
- React Testing Library (`@testing-library/react`)
- Использование моков для API (например `vi.mock()` или `jest.mock()`) и роутера, если это требуется.

НЕ создавай документацию или файлы-отчеты.

После: "✅ Тесты написаны"

РЕЖИМ: Agent Mode